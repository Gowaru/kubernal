# E2E test env — copy to .env.test and adjust if needed
DATABASE_URL=postgresql://kubernal:kubernal_test@localhost:5433/kubernal_test?schema=public
SESSION_SECRET=test-session-secret-must-be-at-least-32-chars-long
CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=silent
PORT=4001
NODE_ENV=test
