apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres
    kubernal.io/managed-by: kubernal
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
